void main() {
    // Initializing data type
    int myNum;

    // Assigning number
    myNum = 5;

    // Declare variable as constant or read-only
    final int myFinalNum = 5;

    // Multiple initialization and declaration of variables
    int x, y, z;
    x = y = z = 5;
}